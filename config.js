/**
 * YOUR REAL SETTINGS — gitignored, never committed.
 */
window.DWF_CONFIG = {

  // Instagram handle, WITHOUT the @
  igHandle: 'goldenmilechrysler',

  // Google review link for the optional "leave a review" button on the
  // thank-you screen. Leave '' to hide it.
  googleReviewUrl: '',

  eventCode: 'DW26',

  // ---- Firebase / Firestore (project: dream-wheels-fest-2026) ----
  // These are NOT secrets — they identify the project. Access is controlled by
  // firestore.rules (create-only, no reads).
  firebase: {
    projectId: 'dream-wheels-fest-2026',
    apiKey: 'AIzaSyDaEfxsBIU5dGa7hxWCTvIAE-Br4C4YV8s',
    collection: 'entries'
  }
};
