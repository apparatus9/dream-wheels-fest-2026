# ---- SECRETS — never commit these ----
# Firebase service account private key. Full admin access to your database.
serviceAccount.json
*-firebase-adminsdk-*.json

# .env (not used, but just in case)
.env
.env.*
!.env.example

# ---- PRIVATE DATA — never publish (has entrant contact info) ----
dashboard.html
entries.csv

# ---- generated / local ----
node_modules/
package-lock.json

# generated by generate-qr.js — rebuild with: node generate-qr.js <live-url>
qr.png
qr.svg
qr.html

.DS_Store
Thumbs.db
*.log

# local preview / scratch files
_*.html

# NOTE: config.js IS committed for GitHub Pages (the Firebase apiKey is not a
# secret — security is enforced by firestore.rules). It is force-added.
