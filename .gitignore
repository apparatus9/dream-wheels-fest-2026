# ---- SECRETS — never commit these ----
# Firebase service account private key. This one is a REAL secret: it grants
# full admin access to your database. If it ever leaks, revoke it in
# Firebase console > Project settings > Service accounts.
serviceAccount.json
*-firebase-adminsdk-*.json

# Local config (Instagram handle, Firebase projectId/apiKey)
config.js
.env
.env.*
!.env.example

# ---- generated / local ----
node_modules/
package-lock.json

entries.csv

# generated by generate-qr.js — rebuild with: node generate-qr.js <live-url>
qr.png
qr.svg
qr.html

.DS_Store
Thumbs.db
*.log
